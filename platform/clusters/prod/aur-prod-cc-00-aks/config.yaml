# Aurora platform Helm chart configuration for Kubernetes deployments.
# Replace all <FILLIN_...> placeholders with environment-specific values before deployment.
# Some sections (for example under core.components) are enabled only when set to enabled: true.

version: '0.0.332'
settings: |-
  global:
    helm: {}
      # Override all Helm applications to pull
      # their helm chart from the following
      # repository.
      #
      # You can override specific charts below
      # in the components section.
      # repository: https://artifacts-artefacts.devops.cloud-nuage.canada.ca/ssc-aurora-helm

    container:
      # Override all applications to pull
      # their container from the following
      # registry.
      #
      # You can override specific charts below
      # in the components section.
      registry: artifacts-artefacts.devops.cloud-nuage.canada.ca/ssc-aurora-docker

    # azure or aws
    provider: azure

    # Name of the Kubernetes cluster.
    cluster: aur-prod-cc-00-aks

    # Domain for ingress.
    ingressDomain: <FILLIN_INGRESS_DOMAIN>

    # Ingress class for Istio.
    ingressClassName: ingress-istio-controller

    # CIDR for the Kubernetes API server.
    apiServerCidr: "<FILLIN_API_SERVER_ADDRESS>/27"

    # The Azure Metadata API
    azureMetadataApiCidr: "169.254.169.254/32"

    # Optional: Uncomment and configure CIDRs for alert manager, logging, and vault if needed for security policies.
    # alertManagerCidrs: ["172.26.26.132/32"]
    # loggingCidrs: ["172.26.26.132/32"]
    # vaultCidrs: ["172.23.211.18/32"]

    # Indicates if Loki logging is enabled cluster-wide.
    clusterHasLoki: true

    # Azure-specific settings.
    load_balancer_subnet_name: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-load-balancer-subnet-name>'
    subscriptionId: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-subscription-id>'
    tenantId: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-tenant-id>'

  ############
  ### CORE ###
  ############
  core:
    enabled: true

    rbac:
      # Allow MSI for firewall rbac
      firewall:
        enabled: true
        msi: "<FILLIN_AZURE_AD_GROUP>"

      platformOperator:
        groups: ["<FILLIN_AZURE_AD_GROUP>"]
      securityOperations:
        groups: ["<FILLIN_AZURE_AD_GROUP>"]

    components:
      #############
      ### Alloy ###
      #############
      alloy:
        enabled: true

        config:
          loki:
            write:
              url: "http://loki-gateway.loki-system.svc.cluster.local/loki/api/v1/push"
              username: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-loki-user>'
              password: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-loki-password>'

      #########################
      ### Aurora Controller ###
      #########################
      auroraController:
        enabled: true

        image:
          tag: main

        prometheus:
          enabled: true
          servicemonitor:
            enabled: true

        aurora:
          secretDockerConfigJson: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-image-pull-secret>'

      ####################
      ### Cert Manager ###
      ####################
      certManager:
        enabled: true

        webhook: {}

        cainjector: {}

        acmesolver: {}

        startupapicheck: {}

        workloadIdentity:
          enabled: true
          clientId: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-certmanager-aadpodidentity-client-id>'

        prometheus:
          enabled: true
          servicemonitor:
            enabled: true

        dns01RecursiveNameservers: "<FILLIN_NAME_SERVERS>:53,<FILLIN_NAME_SERVERS>:53"
        dns01RecursiveNameserversOnly: true

        podDnsConfig:
          nameservers:
            - <FILLIN_NAME_SERVERS>
            - <FILLIN_NAME_SERVERS>

        issuers:
          email: aurora-aurore@ssc-spc.gc.ca
          caBundle: "<FILLIN_BASE&4_CUSTOM_CA>"
          dns01:
            azure:
              zone: <FILLIN_INGRESS_ZONE>.cloud-nuage.canada.ca
              resourceGroupName: <FILLIN_RESOURCE_GROUP_NAME>
              subscriptionId: <FILLIN_SUBSCRIPTION_ID>

      ######################
      ### CIDR Allocator ###
      ######################
      cidrAllocator:
        enabled: false

        image:
          tag: main

        prometheus:
          enabled: true
          servicemonitor:
            enabled: true

        secretDockerConfigJson: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-image-pull-secret>'

        addressPools:
        - <FILL_IN_ADDRESS_POOLS>/23

      ##############
      ### Cilium ###
      ##############
      cilium:
        enabled: false

        secretDockerConfigJson: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-image-pull-secret>'

        enableIPv4Masquerade: true

        prometheus:
          enabled: true
          serviceMonitor:
            enabled: true

        bgpControlPlane:
          enabled: false

        ciliumBGPClusterConfig:
          bgpInstances:
          - localASN: 64512
            name: instance-64512
            peers:
            - peerASN: 64512
              peerAddress: <FILL_IN_PEER_ADDRESS>/32
              name: peer-<FILL_IN_PEER_ADDRESS>

      #############
      ### Falco ###
      #############
      falco:
        enabled: false

        falcoctl:
          artifact:
            install:
              enabled: true
              mounts:
                volumeMounts:
                - name: ca-certificates
                  readOnly: true
                  mountPath: "/etc/ssl/certs/ca-certificates.pem"
                  subPath: "ca-certificates.crt"

        mounts:
          volumes:
          - name: ca-certificates
            configMap:
              name: trust-manager-propagated-custom-ca

      #######################
      ### Fluent Operator ###
      #######################
      fluentOperator:
        enabled: true

        operator:
          initContainer: {}

        fluentd:
          output:
            loki:
              enable: true
              url: "https://loki.<FILLIN_INGRESS_DOMAIN>"
              username: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-loki-user>'
              password: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-loki-password>'

      ##################
      ### Gatekeeper ###
      ##################
      gatekeeper:
        enabled: true

        postInstall:
          probeWebhook: {}

        postUpgrade: {}

        preUninstall: {}

        audit: {}

        controllerManager: {}

        crds: {}

        templates:
          repository: https://github.com/gccloudone-aurora/gatekeeper-policies.git
          path: .
          targetRevision: main

        constraints:
          gatewayContainerAllowedImages:
            allowedRepos:
              - artifacts-artefacts.devops.cloud-nuage.canada.ca/
              - artifacts-artefacts.devops.cloud-nuage.canada.ca:443/
              - ghcr.io/
              - docker.io/
              - docker.elastic.co/
              - mcr.microsoft.com/
              - quay.io/
              - registry.k8s.io/
          restrictPodAnnotations:
            excludedNamespaces:
              - 'backstage'
          systemContainerAllowedImages:
            allowedRepos:
              - artifacts-artefacts.devops.cloud-nuage.canada.ca/
              - artifacts-artefacts.devops.cloud-nuage.canada.ca:443/
              - ghcr.io/
              - docker.io/
              - docker.elastic.co/
              - mcr.microsoft.com/
              - quay.io/
              - registry.k8s.io/
              - icr.io/
          solutionContainerAllowedImages:
            allowedRepos:
              - artifacts-artefacts.devops.cloud-nuage.canada.ca/
              - artifacts-artefacts.devops.cloud-nuage.canada.ca:443/
              - ghcr.io/
              - docker.io/
              - docker.elastic.co/
              - mcr.microsoft.com/
              - quay.io/
              - registry.k8s.io/

      #########################
      ### Grafana Operator ###
      #########################
      grafana:
        enabled: true
        dashboard:
          repository: https://github.com/gccloudone-aurora/grafana-dashboards.git
          path: .
          targetRevision: main
          instanceSelector:
            matchLabels:
              dashboards: aurora

        alerting:
          enabled: true
          repository: https://github.com/gccloudone-aurora/grafana-alerts.git
          path: .
          targetRevision: main
          datasourceUid: prometheus
          instanceSelector:
            matchLabels:
              dashboards: aurora
          folders:
            trivy:
              enabled: true
          timeIntervals:
            weekday-morning:
              enabled: true
          notificationTemplates:
            gcNotify:
              enabled: true
          managedSecrets:
            trivy-gc-notify:
              enabled: true
              stringData:
                trivy-webhook-url: "https://api.notification.canada.ca/v2/notifications/email?="
                trivy-webhook-authorization-credentials: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-grafana-trivy-webhook-auth>'
          contactPoints:
            trivy-gc-notify:
              enabled: true
              recipients:
                - email: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-grafana-dev-email>'
                  name: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-grafana-dev-name>'
                - email: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-grafana-owner-email>'
                  name: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-grafana-owner-name>'
                - email: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-grafana-owner2-email>'
                  name: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-grafana-owner2-name>'
              gcNotify:
                templateId: "<FILLIN_GC_NOTIFY_TEMPLATE_ID>"
              authorization:
                enabled: true
          routes:
            trivy:
              enabled: true

        serverUrl: "https://grafana.<FILLIN_INGRESS_DOMAIN>"
        azure:
          clientId: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-grafana-sp-client-id>'
          clientPassword: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-grafana-sp-client-secret>'
        adminPassword: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-grafana-admin-password>'
        authUrl: "https://login.microsoftonline.com/<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-tenant-id>/oauth2/v2.0/authorize"
        tokenUrl: "https://login.microsoftonline.com/<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-tenant-id>/oauth2/v2.0/token"

        downloadDashboards: {}

        initChownData: {}

        sidecar: {}

        lokiDataSource:
          url: "http://loki-gateway.loki-system.svc.cluster.local"
          user: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-loki-user>'
          password: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-loki-password>'

      #################
      ### KubeBench ###
      #################
      kubebench:
        enabled: true
        cronjob:
          schedule: "30 10 * * *"

      ################
      ### KubeCost ###
      ################
      kubecost:
        enabled: true

      ###########
      ### NTP ###
      ###########
      ntp:
        enabled: true

      ###################
      ### Pod Tracker ###
      ###################
      podtracker:
        enabled: true

        image:
          tag: main

        prometheus:
          enabled: true
          servicemonitor:
            enabled: true

      ##################
      ### Prometheus ###
      ##################
      prometheus:
        enabled: true

        azure:
          clientId: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-grafana-sp-client-id>'
          clientPassword: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-grafana-sp-client-secret>'

        operator:
          admissionWebhooks: {}

          prometheusConfigReloader: {}

          thanos: {}

        alertmanager:
          enabled: true
          alertmanagerSpec:
            replicas: 2
            storage: 2Gi
          config:
            environments:
              prod: {}
              nonProd: {}
              dev:
                matchers:
                  - "class = testing"
                  - "target =~ control-plane-aur-prod-cc-00-aks|istio-ingress-gateway-aur-prod-cc-00-aks"
                  - "cluster =~ aur-prod-cc-00-aks"

        prometheus:
          prometheusSpec:
            additionalAlertManagerHosts:
              - alertmanager-0.aurora.<FILLIN_INGRESS_DOMAIN>
              - alertmanager-1.aurora.<FILLIN_INGRESS_DOMAIN>
            thanos:
              enabled: true
              logLevel: info
              blockSize: 2h
              objectStorage:
                  provider: azure
                  storageAccountName: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-thanos-sa-name>'
                  containerName: 'thanos'
                  createContainer: false # TF handles this
                  endpoint: blob.core.windows.net
                  auth: workloadIdentity
                  clientId: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-thanos-workloadidentity-client-id>'
                  storageAccountKey: "" # Must be blank if using workload identity auth
              sidecar:
                resources:
                  requests:
                    cpu: 250m
                    memory: 500Mi
                  limits:
                    cpu: 500m
                    memory: 512Mi


        thanosRuler:
          thanosRulerSpec: {}

        grafana:
          enabled: false

        kubeStateMetrics: {}

        prometheusNodeExporter: {}

        blackboxExporter:
          istioIngressGatewayProbes:
            - name: istio-ingress-gateway
              ipAddress: <FILLIN_ISTIO_INGRESS_GATEWAY>

        msteams:
          webhooks:
            devCritical: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-alertmanager-msteamsv2-dev-critical>'
            devMajor: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-alertmanager-msteamsv2-dev-major>'

        priorityClassName: platform-cluster-critical

      ################################
      ### Secrets Store CSI Driver ###
      ################################
      secretsStoreCSIDriver:
        enabled: true

      #############
      ### Trivy ###
      #############
      trivy:
        enabled: true

        useAuroraImagePullSecret: true

        resources:
          limits:
            memory: 1Gi

        trivyOperator:
          # We exclude windows based microsoft images as we run on Linux nodes (Trivy will attempt to scan windows versions since image refs deployed by Azure uses both Windows and Linux in same spec)
          # Also exclude auto to ensure istio proxy scans don't fail (proxy submits auto as the image ref, which is fixed when the container runs, but not for Trivy scanning)
          excludeImages: "mcr.microsoft.com/oss/kubernetes-csi/*:*-windows*,mcr.microsoft.com/oss/v2/kubernetes-csi/*:*-windows*,quay.io/prometheus/*,quay.io/prometheus-operator/*,mcr.microsoft.com/oss/azure/aad-pod-identity/*:*-windows*,mcr.microsoft.com/azuredefender/*:*-windows*,mcr.microsoft.com/oss/v2/kubernetes/*:*-windows*,auto"

      ################
      ### Tetragon ###
      ################
      tetragon:
        enabled: true

        prometheus:
          serviceMonitor:
            enabled: true

        policies:
          repository: https://github.com/gccloudone-aurora/tetragon-policies.git
          path: .
          targetRevision: main

      #####################
      ### Trust Manager ###
      #####################
      trustManager:
        enabled: true

        bundle:
          customCA: |-
            <FILLIN_BASE64_CUSTOM_CA>

      ###################
      ### Vault Agent ###
      ###################
      vaultAgent:
        enabled: false

        injector:
          externalVaultAddr: https://vault.<FILLIN_INGRESS_DOMAIN>

          agentImage: {}

      ##############
      ### Velero ###
      ##############
      velero:
        enabled: true

        backupStorage:
          bucket: velero
          resourceGroupName: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-velero-sa-resource-group-name>'
          storageAccountName: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-velero-sa-name>'
          subscriptionId: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-subscription-id>'

        volumeSnapshot:
          resourceGroupName: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-velero-sa-resource-group-name>'

        workloadIdentity:
          enabled: true
          clientId: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-velero-aadpodidentity-client-id>'

  ###########
  ### APP ###
  ###########
  app:
    enabled: true

    components:
      #######################
      ### Argo Controller ###
      #######################
      argoController:
        enabled: true

        controllers:
          syncAppProjects:
            enabled: true
          imagePullSecrets:
            enabled: false
          workflows:
            enabled: false

        storageAccount:
          name: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argo-workflow-sa-name>'
          primaryAccessKey: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argo-workflow-sa-key>'

      #####################
      ### Argo Workflow ###
      #####################
      argoWorkflow:
        enabled: false

        workflow:
          controller: {}

          executor: {}

          server: {}

          storageAccount:
            name: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argo-workflow-sa-name>'
            primaryBlobEndpoint: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argo-workflow-sa-endpoint>'
            primaryAccessKey: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argo-workflow-sa-key>'

          sso:
            issuerUrl: "https://login.microsoftonline.com/<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-tenant-id>/v2.0"
            clientId: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argo-workflow-sp-client-id>'
            clientSecret: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argo-workflow-sp-client-secret>'

      ###############
      ### Elastic ###
      ###############
      elastic:
        enabled: false

      #############
      ### Istio ###
      #############
      istio:
        enabled: true

        gateway:
          additionalDnsNames:
            - '*.<FILLIN_INGRESS_DOMAIN>'
            - '*.aurora.<FILLIN_INGRESS_DOMAIN>'

        # additionalLBPorts:
        #   - name: tcp-postgres
        #     protocol: TCP
        #     port: 5432         # LoadBalancer port
        #     targetPort: 5432   # Envoy / pod port
        # additionalGatewayHosts:
        #   - hosts: ["*"]
        #     port:
        #       name: tcp-postgres
        #       protocol: TCP
        #       number: 5432

        ingressIstioController:
         image:
           tag: main

        istiod:
          pilot: {}

          proxy: {}

          oauth2:
            enabled: true

        # networkPolicy:
        #   ingressGeneral:
        #     extraIngressPorts:
        #       - port: 9090
        #         protocol: TCP
        #       - port: 53
        #         protocol: UDP

      #############
      ### Kiali ###
      #############
      kiali:
        enabled: true

        instances:
          - name: kiali
            auth:
              strategy: anonymous
            grafana:
              token: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-kiali-grafana-token>'

      ##########################
      ### Sidecar Terminator ###
      ##########################
      sidecarTerminator:
        enabled: true

        image:
          tag: main

      ############
      ### Solr ###
      ############
      solr:
         enabled: false

      ###############
      ### Strimzi ###
      ###############
      strimzi:
        enabled: false

  ############
  ### MGMT ###
  ############
  mgmt:
    enabled: true

    components:
      #####################
      ### Argo Operator ###
      #####################
      argoOperator: {}

      #######################
      ### Argo Foundation ###
      #######################
      argoFoundation:
        argocdInstance:
          adminSecurityGroup: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-cluster-admins>'
          disableAdmin: false
          ingressDomain: aur.aurora.<FILLIN_INGRESS_DOMAIN>
          argocdVaultPlugin:
            enabled: true
            env:
              ARGOCD_ENV_AVP_TYPE: "azurekeyvault"
              ARGOCD_ENV_AZURE_CLIENT_ID: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argocd-kv-aadpodidentity-client-id>'
          workloadIdentity:
            enabled: true
            clientId: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argocd-kv-aadpodidentity-client-id>'
          oidcAuthSP:
            credentials:
              tenantID: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-tenant-id>'
              clientID: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argocd-oidc-sp-client-id>'
              clientSecret: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argocd-oidc-sp-client-secret>'
          notifications:
            enabled: true
          netpol:
            enabled: true
          server:
            service:
              type: "ClusterIP"
            resources:
              limits:
                cpu: '1'
                memory: 1000Mi
              requests:
                cpu: 250m
                memory: 500Mi
          register:
            clusters:
              aur-prod-cc-00-aks:
                server: "<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-server>"
                token: "<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argocd-sa-bearer-token>"
                caData: "<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argocd-sa-cacert | base64encode>"
            repositories:
              aurora-helm:
                type: helm
                url: https://gccloudone-aurora.github.io/aurora-platform-charts
              project-aurora-mgmt:
                type: git
                url: https://github.com/gccloudone-aurora/project-aurora-mgmt.git
              project-aurora-sdlc:
                type: git
                url: https://github.com/gccloudone-aurora/project-aurora-sdlc.git
            repositoryCreds:
              private-repo-creds:
                type: git
                url: https://github.com/gccloudone-aurora/project-aurora-mgmt.git
                username: "<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-github-username>"
                password: "<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-github-password>"
              private-repo-creds-sdlc:
                type: git
                url: https://github.com/gccloudone-aurora/project-aurora-sdlc.git
                username: "<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-github-username>"
                password: "<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-github-password>"
        argocdProjects:
          platform:
            applicationSet:
              generators:
                - git:
                    repoURL: https://github.com/gccloudone-aurora/project-aurora-mgmt.git
                    revision: main
                    files:
                      - path: platform/clusters/non-prod/**/config.yaml
                - git:
                    repoURL: https://github.com/gccloudone-aurora/project-aurora-sdlc.git
                    revision: main
                    files:
                      - path: platform/clusters/non-prod/**/config.yaml
              template:
                source:
                  repoURL: https://gccloudone-aurora.github.io/aurora-platform-charts
          namespaces:
            applicationSet:
              generators:
                - git:
                    repoURL: https://github.com/gccloudone-aurora/project-aurora-mgmt.git
                    revision: main
                    files:
                      - path: platform/namespaces/non-prod/**/config.yaml
                - git:
                    repoURL: https://github.com/gccloudone-aurora/project-aurora-sdlc.git
                    revision: main
                    files:
                      - path: platform/namespaces/non-prod/**/config.yaml
              template:
                source:
                  repoURL: https://gccloudone-aurora.github.io/aurora-platform-charts
                  helmValues:
                    argocd:
                      project:
                        sourceRepos: ['*']
          solutions:
            applicationSet:
              generators: []
              template:
                source:
                  repoURL: https://gccloudone-aurora.github.io/aurora-platform-charts
                  helmValues:
                    argocd:
                      project:
                        sourceRepos: ['*']

          syncWindows:
            - clusters:
                - '*'
              duration: 3h
              kind: allow
              manualSync: true
              schedule: '* 19 * * 2,4'
              timeZone: America/Toronto

      #####################
      ### Argo Solution ###
      #####################
      argoSolution:
        enabled: true
        argocdInstance:
          adminSecurityGroup: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-cluster-admins>'
          disableAdmin: false
          ingressDomain: sol.aurora.<FILLIN_INGRESS_DOMAIN>
          oidcAuthSP:
            credentials:
              tenantID: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-tenant-id>'
              clientID: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argocd-oidc-sp-client-id>'
              clientSecret: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argocd-oidc-sp-client-secret>'
          workloadIdentity:
            enabled: true
            clientId: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argocd-kv-sol-workload-identity-client-id>'
          notifications:
            enabled: true
          netpol:
            enabled: true
          server:
            service:
              type: "ClusterIP"
            resources:
              limits:
                cpu: '1'
                memory: 1000Mi
              requests:
                cpu: 250m
                memory: 500Mi
          # applicationSet:
          #   # https://github.com/argoproj-labs/argocd-operator/pull/1988
          #   sourceNamespaces:
          #     - gitops-workload-b
          register:
            clusters:
              aur-prod-cc-00-aks:
                server: "<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-server>"
                token: "<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argocd-sa-bearer-token>"
                caData: "<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-argocd-sa-cacert | base64encode>"
            repositories:
              aurora-helm:
                type: helm
                url: https://gccloudone-aurora.github.io/aurora-platform-charts
            repositoryCreds: {}

        # project:
        #   global:
        #     sourceRepos:
        #       - "https://artifacts-artefacts.devops.cloud-nuage.canada.ca/*"

          sourceNamespaces:
            - "/^((?!-system$).)*$/"

      #######################
      ### Bill of Landing ###
      #######################
      billOfLanding:
        enabled: false

      #############
      ### Loki ###
      ############
      loki:
        enabled: true

        basicAuth:
          htpasswd: "<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-loki-htpasswd>"

        logStorageLocation:
          storageAccountName: "<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-loki-storageaccountname>"
          storageAccountKey: "<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-loki-storageaccountkey>"

      ####################################################
      ### Thanos Query, Compactory, and Store Gateway ####
      ####################################################
      thanos:
        enabled: true
        logLevel: info
        objectStorage:
            auth: workloadIdentity
            # Using a shared bucket and uses the same bucket in the current core sidecar for mgmt
            buckets:
              - name: default
                storageAccountName: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-thanos-sa-name>'
                containerName: 'thanos'
                endpoint: blob.core.windows.net
                clientId: '<path:<FILLIN_ARGO_KV>#<FILLIN_ARGO_KV_PREFIX>-thanos-workloadidentity-client-id>'
        image:
          tag: "v0.41.0"
        query:
          replicas: 1
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: '1'
              memory: 1Gi
        storeGateway:
          replicas: 1
          persistence:
            size: 20Gi
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: '1'
              memory: 2Gi
        compactor:
          retention:
            raw: 30d
            fiveMin: 90d
            oneHour: 1y
          persistence:
            size: 50Gi
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: '1'
              memory: 2Gi
